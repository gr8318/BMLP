import numpy as np
import matplotlib.pyplot as plt

x=np.arange(-10,10,1)
y=np.arange(-10,10,1)

xx,yy=np.meshgrid(x,y)
T=np.array([[-1, 0],[0,-1]])
xy=np.vstack([xx.flatten(),yy.flatten()])
print(xy.shape)

T@xy[:,0]
trans=T@xy

trans.shape
print(xx)
print(yy)
